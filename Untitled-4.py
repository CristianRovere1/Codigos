  alturaret = float(input('Digite a altura do retângulo: '))
baseret = float(input('Digite a base do retângulo: '))
arearet = (alturaret*baseret)
print('A área do retângulo é de ', arearet)

   
